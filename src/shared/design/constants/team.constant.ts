/** How a teammate is marked, in the world and on the map. */
export const TEAM = {
    /** Green, and only ever green: nothing else in the game is this colour. */
    color: '#5ce65c',
    markerWidth: 6,
    markerHeight: 7,
} as const;
