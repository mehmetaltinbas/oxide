/**
 * Master volume out of the box, 0 to 1.
 *
 * Everything is synthesized rather than mixed from files, and the raw tones are
 * hot. 0.35 is where a hatchet stops clipping on a laptop speaker.
 */
export const AUDIO_DEFAULT_LEVEL = 0.35;
