/**
 * Spacing scale, in pixels. Every gap, inset and gutter comes from here, so
 * the interface breathes consistently instead of drifting into fifteen
 * almost-equal paddings. See docs/design-system/design-tokens.md.
 */
export const SPACE = {
    xs: 4,
    sm: 8,
    md: 14,
    lg: 24,
    xl: 40,
} as const;
