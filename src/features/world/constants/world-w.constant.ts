import WORLD_NUMBERS from 'shared/world.json';

// Twice the area again (20736 = 96 x 216 = 64 x 324, so it divides evenly by
// both the biome tile and the build cell).
export const WORLD_W = WORLD_NUMBERS.worldW;
