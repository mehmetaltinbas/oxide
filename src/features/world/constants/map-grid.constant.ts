/**
 * The map grid, Rust's way: columns lettered from A, rows numbered from 0, so
 * a place is called by its square, "F7". Twenty a side puts a square at about
 * a thousand units, a short run across.
 */
export const MAP_GRID_CELLS = 20;
