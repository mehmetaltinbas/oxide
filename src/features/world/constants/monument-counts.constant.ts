/** How many of each monument to scatter, so a big island still has landmarks. */
export const MONUMENT_COUNTS: Record<string, number> = {
    cabins: 8,
    lighthouse: 6,
    airfield: 4,
    powerplant: 3,
};
