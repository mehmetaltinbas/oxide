/** Snow lying on trees and rock in the snow biome. */
export const SNOW = {
    /** Not pure white: a touch of blue so it reads as snow and not as paper. */
    color: '#f2f7fb',
    /** How far down each tier of a tree the snow cap reaches, as a fraction. */
    tierDepth: 0.42,
    /** How far down the visible shelf of a lower tier the snow reaches. */
    shelfDepth: 0.35,
} as const;
