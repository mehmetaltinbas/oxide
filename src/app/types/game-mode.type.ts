/**
 * 'sandbox' is single-player with the survival pressure and the price tags
 * taken off: nothing hurts you, nothing costs anything, and every item in the
 * game is one click away. It exists to test things, not to be played.
 */
export type GameMode = 'single' | 'online' | 'sandbox';
